import fs from "node:fs";

/*
export function fetchAllArt(): Array<string> {
  return fs
    .readdirSync("public/media/art")
    .filter((fileName: string) => fileName.endsWith(".webp"));
}

export function fetchArt(id: string): string | undefined {
  return fs
    .readdirSync("public/media/art")
    .find((fileName) => fileName.startsWith(id));
}
*/
